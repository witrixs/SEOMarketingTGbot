# 🚀 SEOMarketingTGbot — Telegram Bot для автоматизации SEO-рекламы

> Мощный Telegram-бот для расскрутки и автоматизации SEO-рекламы. Планирование постов, управление расписаниями, массовая рассылка и аналитика — всё в одном боте.

---

## 📋 Описание

**SEOMarketingTGbot** — это комплексное решение для автоматизации SEO-рекламы через Telegram. Бот предоставляет администраторам полный контроль над контент-маркетингом:

- 📝 **Создание и управление постами** (текст, фото, видео, GIF)
- ⏰ **Гибкое планирование** (разовая отправка, повторы, еженедельные расписания)
- 👥 **Управление подписчиками** и массовая рассылка
- 📊 **Детальная аналитика** (статистика доставок, новые подписчики)
- 🎯 **Настройка кнопок и ссылок** для каждого поста
- 🔄 **Автоцикл постов** для непрерывной рекламы

---

## 🛠️ Требования

### Python
1. **Python 3.11+**
2. aiogram==3.20.0.post0
3. python-dotenv==1.0.0
4. aiofiles==23.2.1
5. aiosqlite==0.19.0
6. pytz==2025.1

---

## 🚀 Запуск приложения

### 1. Клонирование репозитория

```sh
git clone https://github.com/witrix/SEOMarketingTGbot.git
cd SEOMarketingTGbot
```

### 2. Установка зависимостей

```sh
pip install -r requirements.txt
```

### 3. Настройка окружения

Создайте файл `.env` и заполните переменные:

```env
# Токен Telegram бота (получить у @BotFather)
BOT_TOKEN=your_telegram_bot_token

# ID администраторов (через запятую)
ADMIN_IDS=123456789,987654321

# Путь к базе данных SQLite
DATABASE_PATH=./bot.db

# Глобальная ссылка для всех постов (опционально)
DEFAULT_GLOBAL_LINK=https://your-website.com

# Часовой пояс
TZ=Europe/Moscow
```

### 4. Запуск бота

```sh
python main.py
```

---

## ⚙️ Конфигурация

### Обязательные переменные окружения

| Переменная | Описание | Пример |
|------------|----------|---------|
| `BOT_TOKEN` | Токен Telegram бота | `1234567890:ABCDEF...` |
| `ADMIN_IDS` | ID администраторов | `123456789,987654321` |

### Опциональные переменные

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DATABASE_PATH` | Путь к БД | `./bot.db` |
| `DEFAULT_GLOBAL_LINK` | Глобальная ссылка | `None` |
| `DEFAULT_DESTINATION_CHAT_ID` | Чат для рассылки | `None` |
| `TZ` | Часовой пояс | `Europe/Moscow` |

⚠️ **Никогда не публикуйте свои токены и ключи!**

---

## 📱 Функциональность

### Для администраторов (/admin)

- **📝 Создание постов**: Поддержка текста, фото, видео, GIF с подписями
- **🕒 Планирование**: 
  - Разовая отправка в определенное время
  - Повторы через интервалы (1ч, 3ч, 6ч, 12ч, 24ч)
  - Еженедельные расписания по дням недели
- **🗑️ Управление**: Удаление постов и расписаний
- **✏️ Редактирование**: Изменение еженедельных расписаний
- **📊 Аналитика**: Статистика подписчиков и планируемых постов
- **🔗 Настройки**: Глобальные ссылки и тексты кнопок

### Для пользователей

- **🎯 Приветственный пост** при команде `/start`
- **📈 Автоматическая подписка** на рассылку
- **🎨 Красивое оформление** постов с кнопками

---

## 🗂️ Структура проекта

```
SEOMarketingTGbot/
├── bot/
│   ├── __init__.py
│   ├── main.py              # Основной запуск бота
│   ├── config.py            # Конфигурация
│   ├── db.py               # База данных SQLite
│   ├── models.py           # Модели данных
│   ├── keyboards.py        # Клавиатуры Telegram
│   ├── scheduler.py        # Планировщик задач
│   ├── deps.py            # Зависимости
│   ├── routers/
│   │   ├── admin.py       # Админ-панель
│   │   └── public.py      # Публичные команды
│   └── services/
│       └── posts.py       # Сервис отправки постов
├── media/                 # Медиафайлы
├── main.py               # Точка входа
├── requirements.txt      # Зависимости Python
├── bot.db               # База данных (создается автоматически)
└── README.md
```

---

## 📊 База данных

Бот использует **SQLite** с автоматическими миграциями. Структура таблиц:

- `posts` — контент постов
- `schedules` — разовые расписания  
- `weekly_schedules` — еженедельные расписания
- `subscribers` — пользователи бота
- `destinations` — чаты для рассылки
- `settings` — глобальные настройки
- `post_stats` — статистика доставок

---

## 🔧 Основные команды

### Запуск и разработка
- `python main.py` — запуск бота
- `python -m bot.main` — альтернативный запуск

### Telegram команды
- `/start` — приветствие для пользователей
- `/admin` — вход в админ-панель (только для администраторов)

---

## 📝 Библиотеки

### Backend (Python)
- **aiogram** — асинхронный фреймворк для Telegram Bot API
- **aiosqlite** — асинхронная работа с SQLite
- **python-dotenv** — загрузка переменных окружения
- **aiofiles** — асинхронная работа с файлами

---

## 🎯 Возможности планирования

### Типы расписаний

1. **Разовая отправка**
   - Указание точной даты и времени
   - Опциональные повторы через интервалы

2. **Еженедельные расписания**
   - Выбор дней недели с удобным интерфейсом ✅
   - Указание времени отправки
   - Автоматические повторы каждую неделю

3. **Автоцикл**
   - Автоматическая ротация всех постов
   - Настраиваемый интервал между отправками

---

## 🚀 Развертывание в продакшене

### 1. Использование systemd (Linux)

Создайте файл `/etc/systemd/system/SEOMarketingTGbot.service`:

```ini
[Unit]
Description=SEOMarketing Telegram Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/SEOMarketingTGbot
ExecStart=/usr/bin/python3 main.py
Restart=always
RestartSec=5
Environment=PYTHONPATH=/path/to/SEOMarketingTGbot

[Install]
WantedBy=multi-user.target
```

Запуск:
```sh
sudo systemctl enable SEOMarketingTGbot
sudo systemctl start SEOMarketingTGbot
sudo systemctl status SEOMarketingTGbot
```

### 2. Docker (опционально)

```dockerfile
FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
CMD ["python", "main.py"]
```

---

## 🔧 Расширенная настройка

### Медиафайлы

Разместите изображения в папке `media/`:
- `Global-post.png` — картинка для приветственного поста

### Кастомизация

- **Дефолтный текст кнопки**: `bot/services/posts.py` → `DEFAULT_BUTTON_TEXT`
- **Дефолтный пост текст**: `bot/main.py` → `default_text`
- **Приветственный текст**: `bot/routers/public.py` → `default_text`
- **Стартовый контент**: `bot/main.py` → функция создания дефолтного поста

---

## 🐛 Решение проблем

### Частые ошибки

1. **Бот не отвечает** — проверьте правильность `BOT_TOKEN`
2. **Нет прав администратора** — убедитесь, что ваш ID в `ADMIN_IDS`
3. **Ошибки базы данных** — проверьте права на запись в `DATABASE_PATH`
4. **Проблемы с расписанием** — проверьте часовой пояс `TZ`

### Логи

Для отладки добавьте в начало `main.py`:

```python
import logging
logging.basicConfig(level=logging.INFO)
```

---

## 🤝 Участие в разработке

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле `LICENSE`.

---

## ❤️ Dev by witrix

Разработано с ❤️ для эффективной автоматизации SEO-рекламы через Telegram.

---

⭐ **Поставьте звезду, если проект оказался полезным!**
